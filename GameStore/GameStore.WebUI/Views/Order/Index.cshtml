@using GameStore.WebUI.Models
@using GameStore.Domain.Entities
@model IEnumerable<Order>

@{
    ViewBag.Title = "Заказы";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="panel panel-primary">
    <div class="panel-heading">Заказы</div>
    <table class="table table-striped">
        <tr>
            <th>Адрес</th>
            <th>Город</th>
            <th>Страна</th>
            <th>Статус</th>
            <th>Время</th>
            <th>Цена</th>
            @*<th style="min-width: 150px"></th>*@
        </tr>
        @if (Model.Count() == 0)
        {
            <tr>
                <td colspan="6" class="text-center">Нет заказов</td>
            </tr>
        }
        else
        {
            foreach (Order order in Model)
            {
                <tr>
                    <td>@order.Adress</td>
                    <td>@order.Town</td>
                    <td>@order.Country</td>
                    <td>
                        @switch (order.Status)
                        {
                            case 0:@:На рассмотрении
                                break;
                            case 1:@:Отправлен
                                break;
                            case 2:@:Получен
                                break;
                            default:@:Ошибка
                                break;
                        }
                        @*@if (order.Status == 0)
                            {
                                @:На рассмотрении
                            }*@
                    </td>
                    <td>@order.Date</td>
                    <td>@order.Price</td>
                </tr>
            }
        }
    </table>
</div>


@*<h3>@o.RecipientName</h3>
    <p>@o.Adress</p>
    <p>@o.Town</p>
    <p>@o.Country</p>
    <h4>
        @if (o.Status == 0)
        {<p>test</p>}
    </h4>
    <h4>@o.Date</h4>
    <h4>@o.Price.ToString("# руб")</h4>*@

@*@model GameStore.Domain.Entities.Order*@